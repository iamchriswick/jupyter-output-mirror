{
  "name": "jupyter-output-mirror",
  "displayName": "Jupyter Output Mirror",
  "description": "Mirror Jupyter Notebook stdout/stderr to a VS Code Output channel.",
  "publisher": "iamchriswick",
  "version": "0.0.4",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Notebooks"
  ],
  "main": "./out/extension.js",
  "extensionDependencies": [
    "ms-toolsai.jupyter",
    "ms-python.python"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onNotebook:*"
  ],
  "contributes": {
    "commands": [
      {
        "command": "jupyterOutputMirror.toggle",
        "title": "Jupyter Output Mirror: Toggle"
      },
      {
        "command": "jupyterOutputMirror.clear",
        "title": "Jupyter Output Mirror: Clear Output Channel(s)"
      },
      {
        "command": "jupyterOutputMirror.createDemo",
        "title": "Jupyter Output Mirror: Create Demo Notebook"
      },
      {
        "command": "jupyterOutputMirror.showQuickStart",
        "title": "Jupyter Output Mirror: Open QuickStart"
      }
    ],
    "configuration": {
      "title": "Jupyter Output Mirror",
      "properties": {
        "jupyterOutputMirror.channelMode": {
          "type": "string",
          "enum": [
            "single",
            "perNotebook"
          ],
          "default": "single",
          "description": "Use one Output channel for everything or a separate channel per notebook."
        },
        "jupyterOutputMirror.debounceMs": {
          "type": "number",
          "default": 50,
          "minimum": 0,
          "maximum": 5000,
          "description": "Debounce window (ms) to coalesce rapid output changes. Set 0 to disable."
        },
        "jupyterOutputMirror.clearOnNotebookClear": {
          "type": "boolean",
          "default": false,
          "description": "When a notebook's outputs are cleared, also clear its mirrored Output channel."
        },
        "jupyterOutputMirror.includeTextPlain": {
          "type": "boolean",
          "default": false,
          "description": "Also mirror text/plain outputs (not just stdout/stderr)."
        },
        "jupyterOutputMirror.timestamp": {
          "type": "boolean",
          "default": true,
          "description": "Prefix each chunk with an ISO timestamp."
        },
        "jupyterOutputMirror.activeNotebookOnly": {
          "type": "boolean",
          "default": true,
          "description": "Mirror only from the currently active notebook editor."
        },
        "jupyterOutputMirror.stripAnsi": {
          "type": "boolean",
          "default": true,
          "description": "Strip ANSI color codes before appending to Output."
        },
        "jupyterOutputMirror.maxChunkSize": {
          "type": "number",
          "default": 8192,
          "minimum": 100,
          "maximum": 100000,
          "description": "Maximum characters per output chunk (larger chunks may be split)."
        },
        "jupyterOutputMirror.debug": {
          "type": "boolean",
          "default": false,
          "description": "Write verbose debug messages to Debug Console and the Output channel."
        },
        "jupyterOutputMirror.includeSourceContext": {
          "type": "boolean",
          "default": true,
          "description": "Include source context line (e.g. \"[stdout] path/to/notebook.ipynb #cell\") before outputs."
        }
      }
    }
  },
  "scripts": {
    "compile": "npm run clean && npm run build:ts && npm run copy:assets",
    "build:ts": "tsc -p .",
    "copy:assets": "mkdir -p out/assets && cp src/*.html out/assets/ 2>/dev/null || true",
    "watch": "tsc -w -p .",
    "package": "npx @vscode/vsce package",
    "clean": "rm -rf out"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@vscode/vsce": "^3.1.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@types/vscode": "^1.84.0"
  }
}
