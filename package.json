{
  "name": "jupyter-output-mirror",
  "displayName": "Jupyter Output Mirror",
  "description": "Mirror Jupyter Notebook stdout/stderr to a VS Code Output channel.",
  "publisher": "yourid",
  "version": "0.0.2",
  "engines": {
    "vscode": "^1.84.0"
  },
  "categories": [
    "Notebooks"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished",
    "onNotebook:*"
  ],
  "contributes": {
    "commands": [
      {
        "command": "jupyterOutputMirror.toggle",
        "title": "Jupyter Output Mirror: Toggle"
      },
      {
        "command": "jupyterOutputMirror.clear",
        "title": "Jupyter Output Mirror: Clear Output Channel(s)"
      }
    ],
    "configuration": {
      "title": "Jupyter Output Mirror",
      "properties": {
        "jupyterOutputMirror.channelMode": {
          "type": "string",
          "enum": [
            "single",
            "perNotebook"
          ],
          "default": "single",
          "description": "Use one Output channel for everything or a separate channel per notebook."
        },
        "jupyterOutputMirror.debounceMs": {
          "type": "number",
          "default": 50,
          "minimum": 0,
          "maximum": 5000,
          "description": "Debounce window (ms) to coalesce rapid output changes. Set 0 to disable."
        },
        "jupyterOutputMirror.clearOnNotebookClear": {
          "type": "boolean",
          "default": false,
          "description": "When a notebook's outputs are cleared, also clear its mirrored Output channel."
        },
        "jupyterOutputMirror.includeTextPlain": {
          "type": "boolean",
          "default": false,
          "description": "Also mirror text/plain outputs (not just stdout/stderr)."
        },
        "jupyterOutputMirror.timestamp": {
          "type": "boolean",
          "default": true,
          "description": "Prefix each chunk with an ISO timestamp."
        },
        "jupyterOutputMirror.activeNotebookOnly": {
          "type": "boolean",
          "default": true,
          "description": "Mirror only from the currently active notebook editor."
        },
        "jupyterOutputMirror.stripAnsi": {
          "type": "boolean",
          "default": true,
          "description": "Strip ANSI color codes before appending to Output."
        },
        "jupyterOutputMirror.maxChunk": {
          "type": "number",
          "default": 65536,
          "minimum": 1024,
          "description": "Maximum characters appended per change to prevent floods."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
    "package": "npx @vscode/vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "@types/vscode": "1.84.0",
    "typescript": "^5.4.5",
    "@vscode/vsce": "^2.15.0"
  }
}